# Node
node_modules/

# Build output (dist is committed so npm install from git works)
# dist/

# Source folders - plugin distributes via dist/ only
admin/
server/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local

# Optional: lock file (if you don't want to push it)
# package-lock.json
